# Руководство по галерее изображений

## Обзор
Добавлена функциональность модального окна для просмотра изображений в полном размере с возможностью сохранения.

## Новые возможности

### 🖼️ Модальное окно просмотра изображений
- **Полноэкранный просмотр** - изображения отображаются в полном размере
- **Навигация** - переключение между изображениями стрелками или миниатюрами
- **Сохранение** - кнопка для скачивания изображений
- **Клавиатурное управление** - ESC для закрытия, стрелки для навигации

### 📱 Адаптивный дизайн
- Оптимизирован для мобильных устройств
- Адаптивные размеры кнопок и изображений
- Удобная навигация на сенсорных экранах

## Обновленные компоненты

### 1. ImageModal.jsx (новый компонент)
**Функциональность:**
- Модальное окно с полноэкранным просмотром
- Навигация между изображениями
- Миниатюры для быстрого перехода
- Кнопка сохранения изображений
- Поддержка клавиатурных сокращений

**Пропсы:**
- `isOpen` - состояние открытия модального окна
- `onClose` - функция закрытия
- `images` - массив URL изображений
- `currentIndex` - индекс текущего изображения
- `title` - заголовок модального окна

### 2. SingleArtist.jsx
- Галерея изображений артиста теперь кликабельна
- При клике открывается модальное окно
- Заголовок: "Галерея [Имя артиста]"

### 3. SinglePerformance.jsx
- Галерея изображений спектакля теперь кликабельна
- При клике открывается модальное окно
- Заголовок: "Галерея спектакля '[Название]'"

### 4. TheatreCollage.jsx
- Все изображения истории театра теперь кликабельны
- При клике открывается модальное окно
- Заголовок: "История театра"

## CSS стили

### Новые классы:
- `.image-modal-overlay` - фон модального окна
- `.image-modal-content` - контейнер модального окна
- `.image-modal-image` - основное изображение
- `.image-modal-thumbnail` - миниатюры
- `.gallery-image` - изображения в галереях
- `.gallery-image:hover` - эффект при наведении

### Эффекты:
- **Hover эффекты** - увеличение и подсветка при наведении
- **Плавные переходы** - анимации при взаимодействии
- **Тени** - объемные тени для глубины
- **Скругленные углы** - современный дизайн

## Как использовать

### Для пользователей:
1. **Открытие изображения** - кликните на любое изображение в галерее
2. **Навигация** - используйте стрелки или миниатюры внизу
3. **Сохранение** - нажмите кнопку 💾 для скачивания
4. **Закрытие** - нажмите ✕ или ESC

### Для разработчиков:
```jsx
import ImageModal from '../components/ImageModal';

const [isModalOpen, setIsModalOpen] = useState(false);
const [selectedImageIndex, setSelectedImageIndex] = useState(0);

const openImageModal = (index) => {
    setSelectedImageIndex(index);
    setIsModalOpen(true);
};

<ImageModal
    isOpen={isModalOpen}
    onClose={() => setIsModalOpen(false)}
    images={imageArray}
    currentIndex={selectedImageIndex}
    title="Название галереи"
/>
```

## Клавиатурные сокращения
- **ESC** - закрыть модальное окно
- **←** - предыдущее изображение
- **→** - следующее изображение

## Поддерживаемые форматы
- JPEG (.jpg, .jpeg)
- PNG (.png)
- WebP (.webp)
- GIF (.gif)

## Адаптивность
- **Desktop** - полнофункциональный интерфейс
- **Tablet** - оптимизированные размеры
- **Mobile** - упрощенная навигация, сенсорное управление

## Браузерная совместимость
- Chrome 60+
- Firefox 55+
- Safari 12+
- Edge 79+

## Производительность
- Ленивая загрузка изображений
- Оптимизированные размеры миниатюр
- Плавные анимации с CSS transitions
- Эффективное управление состоянием

## Безопасность
- Безопасное скачивание файлов
- Валидация URL изображений
- Защита от XSS атак



