# Руководство по галерее изображений в новостях

## Обзор
Добавлена поддержка галереи изображений в компонентах новостей, аналогично галереям артистов и спектаклей.

## Новые возможности

### 📰 Галерея в новостях
- **Полная галерея** - в SingleNews отображается полная галерея изображений
- **Мини-галерея** - в списке новостей показываются первые 3 изображения
- **Модальное окно** - клик на любое изображение открывает полноэкранный просмотр
- **Сохранение** - возможность скачивания изображений

### 🎨 Адаптивный дизайн
- Мини-галерея в списке новостей
- Полная галерея на странице новости
- Оптимизировано для мобильных устройств

## Обновленные компоненты

### 1. API (starpi.js)
**Обновлено:**
- `fetchNewsData()` - добавлено поле `gallery`
- `fetchNewsPage()` - добавлено поле `gallery`

**Новые поля:**
```javascript
{
  id: item.id,
  title: item.title,
  date: item.date,
  text: item.text,
  image: item.image,        // Главное изображение
  gallery: item.gallery     // Массив изображений галереи
}
```

### 2. SingleNews.jsx
**Новые возможности:**
- Отображение полной галереи изображений
- Модальное окно для просмотра
- Навигация между изображениями
- Кнопка сохранения

**Структура:**
```jsx
{/* Галерея изображений */}
{news.gallery && news.gallery.length > 0 && (
    <div className="news-gallery">
        <h3>Галерея изображений</h3>
        <div className="gallery-images">
            {news.gallery.map((imageUrl, index) => (
                <img
                    key={index}
                    src={imageUrl}
                    alt={`Изображение ${index + 1}`}
                    className="gallery-image"
                    onClick={() => openImageModal(index)}
                />
            ))}
        </div>
    </div>
)}
```

### 3. NewsItem.jsx
**Новые возможности:**
- Мини-галерея (первые 3 изображения)
- Счетчик дополнительных изображений
- Модальное окно для просмотра
- Кликабельные миниатюры

**Структура:**
```jsx
{/* Мини-галерея изображений */}
{gallery && gallery.length > 0 && (
    <div className="news-mini-gallery">
        <h4>Галерея</h4>
        <div className="mini-gallery-images">
            {gallery.slice(0, 3).map((imageUrl, index) => (
                <img
                    key={index}
                    src={imageUrl}
                    alt={`Изображение ${index + 1}`}
                    className="mini-gallery-image"
                    onClick={() => openImageModal(index)}
                />
            ))}
            {gallery.length > 3 && (
                <div className="mini-gallery-more">
                    +{gallery.length - 3}
                </div>
            )}
        </div>
    </div>
)}
```

### 4. News.jsx
**Обновлено:**
- Передача поля `gallery` в NewsItem
- Поддержка галереи в списке новостей

## CSS стили

### Новые классы:
- `.news-gallery` - контейнер полной галереи
- `.news-mini-gallery` - мини-галерея в списке
- `.mini-gallery-images` - контейнер миниатюр
- `.mini-gallery-image` - миниатюры изображений
- `.mini-gallery-more` - счетчик дополнительных изображений

### Стили галереи:
```css
.news-gallery {
    margin-top: 2rem;
    text-align: center;
}

.news-gallery h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: #333;
    font-weight: 600;
}
```

### Стили мини-галереи:
```css
.mini-gallery-image {
    width: 60px;
    height: 60px;
    object-fit: cover;
    border-radius: 6px;
    cursor: pointer;
    transition: all 0.3s ease;
}

.mini-gallery-image:hover {
    transform: scale(1.1);
    border-color: #007bff;
}
```

## Как использовать

### В Strapi:
1. Откройте раздел "News" в админке
2. Создайте или отредактируйте новость
3. В поле "Gallery" загрузите изображения
4. Сохраните изменения

### Для пользователей:
1. **В списке новостей** - кликните на миниатюры в галерее
2. **На странице новости** - кликните на любое изображение в полной галерее
3. **Навигация** - используйте стрелки или миниатюры
4. **Сохранение** - нажмите кнопку 💾

## Структура данных

### Strapi Content Type:
```json
{
  "attributes": {
    "title": { "type": "string" },
    "date": { "type": "date" },
    "image": { "type": "media", "multiple": false },
    "text": { "type": "richtext" },
    "gallery": { 
      "type": "media", 
      "multiple": true,
      "allowedTypes": ["images"]
    }
  }
}
```

### API Response:
```javascript
{
  id: 1,
  title: "Заголовок новости",
  date: "2024-01-15",
  text: "Текст новости в Markdown",
  image: "http://localhost:1337/uploads/image.jpg",
  gallery: [
    "http://localhost:1337/uploads/gallery1.jpg",
    "http://localhost:1337/uploads/gallery2.jpg",
    "http://localhost:1337/uploads/gallery3.jpg"
  ]
}
```

## Особенности

### Мини-галерея в списке:
- Показывает только первые 3 изображения
- Счетчик "+N" для дополнительных изображений
- Компактный дизайн для экономии места

### Полная галерея на странице:
- Все изображения в полном размере
- Модальное окно для детального просмотра
- Навигация и сохранение

### Адаптивность:
- Мини-галерея адаптируется под размер экрана
- Полная галерея оптимизирована для мобильных
- Сенсорное управление на планшетах

## Производительность
- Ленивая загрузка изображений
- Оптимизированные размеры миниатюр
- Эффективное управление состоянием модального окна
- Плавные анимации с CSS transitions

## Браузерная совместимость
- Chrome 60+
- Firefox 55+
- Safari 12+
- Edge 79+
- Мобильные браузеры

## Безопасность
- Валидация URL изображений
- Безопасное скачивание файлов
- Защита от XSS атак
- Проверка типов файлов



