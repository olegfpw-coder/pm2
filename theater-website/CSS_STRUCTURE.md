# Структура CSS

## Обзор

Стили проекта разделены на логические модули для улучшения читаемости, поддержки и организации кода.

## Структура папок

```
src/styles/
├── base/                 # Базовые стили
│   ├── variables.css     # CSS переменные
│   ├── reset.css         # Сброс стилей
│   └── typography.css    # Типографика
├── components/           # Стили компонентов
│   ├── buttons.css       # Система кнопок
│   ├── header.css        # Шапка и навигация
│   ├── footer.css        # Футер
│   └── slider.css        # Слайдер
├── pages/                # Стили страниц
│   ├── home.css          # Главная страница
│   ├── about.css         # О театре
│   ├── news.css          # Новости
│   ├── performances.css  # Спектакли
│   ├── afisha.css        # Афиша
│   └── artists.css       # Артисты и команда
├── utils/                # Утилитарные стили
│   ├── responsive.css    # Адаптивность
│   └── accessibility.css # Доступность
└── main.css              # Главный файл импортов
```

## Главный файл (main.css)

Все стили импортируются через `src/styles/main.css`. Этот файл является единой точкой входа для всех стилей проекта.

### Порядок импорта:
1. **Базовые стили** (переменные, сброс, типографика)
2. **Компоненты** (кнопки, хедер, футер, слайдер)
3. **Страницы** (специфичные стили для каждой страницы)
4. **Утилиты** (адаптивность, доступность)

## CSS Переменные

Все переменные определены в `base/variables.css`:

### Цвета
- `--primary`: Основной цвет (золотой)
- `--accent`: Акцентный цвет (синий)
- `--text`: Цвет текста
- `--bg-dark`: Темный фон

### Размеры
- `--spacing-xs` до `--spacing-3xl`: Отступы
- `--font-size-xs` до `--font-size-5xl`: Размеры шрифтов
- `--radius`, `--radius-sm`, `--radius-lg`: Скругления

### Эффекты
- `--shadow`, `--shadow-sm`, `--shadow-lg`: Тени
- `--transition`, `--transition-fast`: Переходы

## Система кнопок

Унифицированная система кнопок в `components/buttons.css`:

### Варианты:
- `btn--primary`: Основная кнопка
- `btn--secondary`: Вторичная кнопка
- `btn--outline`: Контурная кнопка
- `btn--ghost`: Призрачная кнопка

### Размеры:
- `btn--sm`: Маленькая кнопка
- `btn--lg`: Большая кнопка

### Обратная совместимость:
Старые классы (`.buy-ticket-btn`, `.news-btn` и т.д.) автоматически применяют новые стили.

## Адаптивность

Все адаптивные стили собраны в `utils/responsive.css`:

### Брейкпоинты:
- Mobile: до 767px
- Tablet: 768px - 1023px
- Desktop: 1024px+

### Утилиты:
- `.hidden-mobile`: Скрыть на мобильных
- `.hidden-desktop`: Скрыть на десктопах
- `.responsive-container`: Адаптивный контейнер

## Доступность

Стили доступности в `utils/accessibility.css`:

### Возможности:
- Высокий контраст
- Увеличенные шрифты
- Улучшенный фокус
- Поддержка screen readers

## Рекомендации по использованию

### 1. Добавление новых стилей

**Для нового компонента:**
```css
/* src/styles/components/new-component.css */
.new-component {
    background-color: var(--card-bg);
    padding: var(--spacing-md);
    border-radius: var(--radius);
}
```

**Затем добавьте импорт в main.css:**
```css
@import './components/new-component.css';
```

### 2. Изменение существующих стилей

Найдите соответствующий файл по названию компонента или страницы и внесите изменения там.

### 3. Глобальные изменения

Для изменения цветов, размеров или других глобальных параметров используйте `base/variables.css`.

## Миграция с старого styles.css

✅ **Выполнено:**
- Разделение монолитного CSS файла
- Создание модульной структуры
- Обновление основных импортов
- Унификация системы кнопок
- Улучшение CSS переменных

🔄 **Требует внимания:**
Некоторые файлы все еще могут содержать импорты старого `styles.css`. Замените их на:
```javascript
// Стили импортируются в main.css
```

## Производительность

### Преимущества новой структуры:
1. **Лучшая организация**: Легче найти нужные стили
2. **Избежание дублирования**: Общие стили вынесены в переменные
3. **Модульность**: Каждый файл отвечает за свою область
4. **Поддержка**: Проще вносить изменения и исправления
5. **Масштабируемость**: Легче добавлять новые компоненты

### Tree-shaking:
CSS модули позволяют в будущем подключать только нужные стили.

## Дальнейшие улучшения

1. **CSS Modules**: Можно перейти на CSS модули для избежания конфликтов имен
2. **CSS-in-JS**: Рассмотреть styled-components или emotion
3. **PostCSS**: Добавить автопрефиксы и оптимизацию
4. **Критический CSS**: Выделить критические стили для первой загрузки

## Заключение

Новая структура CSS делает проект более организованным и удобным для разработки. Каждый модуль отвечает за свою область, что упрощает поиск и изменение стилей.









